[package]
name = "velocity-mcp-client"
version = "3.2.0"
edition = "2021"
authors = ["UnitBuilds <ian@unitbuilds.com>"]
description = "Client SDK for VELOCITY-MCP servers"
license = "MIT OR Apache-2.0"
repository = "https://github.com/UnitBuilds-CC/V.E.L.O.C.I.T.Y.-MCP"
keywords = ["mcp", "client", "sdk", "ai", "llm"]
categories = ["api-bindings", "development-tools"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2"
uuid = { version = "1.0", features = ["v4"] }
async-trait = "0.1"
memmap2 = "0.9"
sha2 = "0.10"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
tokio = { version = "1", default-features = false, features = ["full"] }
reqwest = { version = "0.11", features = ["json"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
tokio = { version = "1", default-features = false, features = ["sync", "macros", "io-util", "rt", "time"] }
reqwest = { version = "0.11", features = ["json"] }

[dev-dependencies]
tokio-test = "0.4"
