{
  "name": "wasm-julia-tools",
  "version": "1.0.0",
  "tools": [
    {
      "name": "julia_greet",
      "description": "Greeting tool powered by the Julia/WASM interpreter. Accepts a name argument and returns a personalized greeting.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Name to greet"
          }
        },
        "required": ["name"]
      },
      "executor": {
        "executor_type": "wasm",
        "language": "julia",
        "source": "return \"Hello, $name!\"",
        "timeout": 5
      }
    }
  ]
}
