{
  "name": "wasm-perl-tools",
  "version": "1.0.0",
  "tools": [
    {
      "name": "perl_hello",
      "description": "Demo greeting from the minimal Perl/WASM interpreter. This runtime is a simplified script interpreter: a built-in demo wrapper runs at call time (tool source is not executed) and arguments are not passed through.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "Unused placeholder argument (not passed to the interpreter)"
          }
        },
        "required": ["text"]
      },
      "executor": {
        "executor_type": "wasm",
        "language": "perl",
        "source": "return \"perl tool ready\";",
        "timeout": 5
      }
    }
  ]
}
